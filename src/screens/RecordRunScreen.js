import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const RecordRunScreen = () => {
  return (
    <View>
        <Text>Record Run Screen</Text>
    </View>
  );  
};

const styles = StyleSheet.create({
    container:{
      
    }
});

export default RecordRunScreen;